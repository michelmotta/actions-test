language: node_js

node_js:
 - stable

services:
  - postgresql

env:
  global:
    - POSTGRES_HOST=localhost
    - POSTGRES_PORT=5432

addons:
  sonarcloud:
    organization: "michelmotta"
    token:
      secure: "b69617cf732223f2c4121799d42bd829fa915079"

install:
  - npm install

script:
  - npm test
  - sonar-scanner